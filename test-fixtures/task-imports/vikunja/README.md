This directory contains a fully synthetic Vikunja export fixture. No record is
derived from a real account.

The source files remain uncompressed so reviewers and secret scanners can
inspect them. To exercise an archive upload manually, create a ZIP whose root
contains `VERSION`, `filters.json`, and `data.json` from
`synthetic-vikunja-export/`.
