Task import fixtures live here.

Use one folder per source application. Keep fixtures small enough for local and
e2e runs, but varied enough to cover labels, due dates, priorities,
recurrence, and source-specific grouping behavior.
